# /action.yml
name: 'Compile apache'
description: 'add actions'
inputs:
  target-os-image:
    description: 'target os image info'
runs:
  using: 'docker'
  image: Dockerfile
  env:
    target-os-image: ${{ inputs.target-os-image }}